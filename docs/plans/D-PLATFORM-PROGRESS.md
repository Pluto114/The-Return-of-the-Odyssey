# D 角色项目进度

本文记录翁晨昊负责的平台、Bot、数据持久化和可观测性工作。记录日期为 2026 年 9 月 14 日，功能代码基线为 `09dbf76`，工作分支为 `feature/week2-platform`，对照主分支为 `a68acfc`。

## 当前判断

当前分支可以直接快进到主分支，没有文本冲突。Server、Bot、Dashboard 和 C++ 客户端均已完成本机构建或测试，真实 Redis 与 MySQL 集成也已通过。这个状态适合继续放在功能分支供团队联调，暂时不应标记成最终可发布版本。

还剩两类工作。第一类是 D 自己能够处理的结算关闭安全问题。第二类依赖 A 完成奖励、三关和恢复的正式入口。后一类完成以后，D 才能继续跑三关 Bot、恢复场景和持续负载。

## 工作进度

| 工作项 | 当前状态 | 已经做到的内容 | 还缺什么 |
| --- | --- | --- | --- |
| D1 装备配置与启动装配 | 已完成 | 服务端启动时校验版本 1 装备目录，装配奖励时长、关卡上限、首关 Seed 和 Director 参数。客户端显示表从同一目录生成 | 等 A2 和 A3 使用这些配置跑完奖励与三关 |
| D2 Bot 三关、异常与持续负载 | 实现完成，真实多关联调待推进 | Bot 已支持权威战斗、奖励选择、异常选择、Ready、药水、Resume 和持续补局。真实服务端已通过 2 Bot 和 10 Bot 单关 | A2 和 A3 接通后跑 10 Bot 三关，再跑 100 Bot、50 房间、10 分钟 |
| D3 指标、Admin 与看板 | 单关范围已验证 | Prometheus、Admin HTTP 与 WebSocket、Dashboard 和 Grafana 已接入真实权威数据。10 Bot 运行时抓到 10 人、5 房间、15 怪物和 28 投射物 | 奖励、Director 和 Resume 的正式事件仍要等 A2 至 A4 接线后复验 |
| D4 Redis Resume 存储 | D 侧完成 | Token 键经过 SHA-256 处理，支持单次消费、TTL、路由版本、撤销和启动连通检查。真实 Redis 集成已通过 | A4 负责签发与轮换 Token、连接代次校验、首快照和宽限期移除 |
| D5 MySQL 异步幂等结算 | 主体完成，关闭安全待修 | 已有迁移、结果与玩家终局表、有界异步队列、幂等冲突检测、重试、JSONL 死信、生产装配和指标。胜利、团灭、离弃、重复提交及数据库故障测试已通过 | 修复退出超时可能遗漏已接收结果的问题。A3 和 A4 还要在正式终局调用 `ResultWriter.Submit` |

## 已有验证证据

| 验证项 | 结果 |
| --- | --- |
| Server 和 Bot 全包 `go test` | 通过 |
| Server 和 Bot 全包 `go vet` | 通过 |
| Dashboard 生产构建 | 通过 |
| C++ 客户端构建 | 通过 |
| CTest | 4 项全部通过 |
| 真实 Redis 与 MySQL 集成 | 通过，四个 Compose 服务均为 Healthy |
| 2 Bot 单关 | 2 个成功，0 个失败，最后清场 Tick 为 32 |
| 10 Bot 单关 | 10 个成功，0 个失败，形成 5 个真实房间 |
| 真实战斗指标 | 峰值 15 个怪物、28 个投射物，累计伤害 600，清场计数 5 |
| production 启动冒烟 | `/healthz` 返回 `ok`，结果队列指标可以抓取 |

原始记录放在 [D 平台阶段验收](../verification/phase2-final/D-PLATFORM-PARTIAL.md)、[2 Bot 单关输出](../verification/phase2-final/d2-two-bot-one-stage.json) 和 [10 Bot 指标输出](../verification/phase2-final/d3-ten-bot-live-metrics.json)。

协议生成脚本本次没有跑完。脚本安装固定版本 `protoc-gen-go` 时访问 `proxy.golang.org` 超时。本分支没有修改 `proto/`，现有生成代码可以正常编译。正式合并验收仍需在网络正常的环境重跑生成脚本。

Race 尚未执行。项目负责人已经决定在开发完成并形成合并候选以后统一执行，本记录不把它算作通过。

## 合并前必须处理的风险

`ResultWriter.Shutdown` 在排空超时后会取消内部 Context。正在等待重试的结果可能直接返回，队列里的其余结果也可能随进程退出消失。这些结果已经被 `Submit` 接收，却没有写进 MySQL 或死信文件。

这个问题不会造成当前编译失败，却会破坏结算可靠性。建议把它列为 D 的下一项最高优先级工作。修复时要将正在处理和仍在队列中的信封交给独立、有时限的死信 Context，随后再关闭数据库与文件。需要补三组测试，分别覆盖重试等待时关闭、队列仍有多条结果时关闭、死信写入本身失败。

## 接下来的工作建议

| 顺序 | 负责人 | 建议工作 | 完成标准 |
| --- | --- | --- | --- |
| 1 | D | 修复 ResultWriter 退出超时的数据遗漏 | 所有已接收结果要么写库，要么进入死信。测试能稳定覆盖队列和处理中结果 |
| 2 | A 与 D | 冻结奖励和结算交接点 | A2 的奖励事件只消费一次，A3 在 Room 销毁前生成稳定 `match_id` 并调用 D 的写入边界 |
| 3 | A | 完成奖励、Ready、Director 和三关入口 | 正式 gameserver 可以完成固定 Seed 三关，重复 Ready 和异常奖励不会污染状态 |
| 4 | A 与 D | 完成 Resume 接线 | Token 能轮换，旧连接失效，宽限期内恢复身份、房间、HP 和装备，过期后移除 |
| 5 | D | 跑功能与异常验收 | 10 Bot 完成三关，合法、非法、重复和超时奖励场景都有独立结果 |
| 6 | D 与 B | 跑持续负载 | 100 Bot、50 房间持续 10 分钟，成功率和 Tick 指标达到周计划门槛，结束后计数归零 |
| 7 | 全组 | 形成合并候选 | 重跑协议生成、Go Race、全部 CTest、Dashboard、真实数据库集成和干净 clone 演练 |

## 建议的提交与合并方式

当前提交继续保留在 `feature/week2-platform`。D 修好关闭安全问题后再推一个独立提交，便于 B 审查持久化语义。A2 至 A4 完成以后，将 A 与 D 的分支放进共同候选分支解决入口冲突并跑完整验收。最终验收通过后再合入 `main`。

如果团队现在需要一个可共享的单关集成基线，可以从当前功能分支创建 PR。PR 描述应明确写出单关已通过，三关、恢复、Race 和最终发布仍未完成。
