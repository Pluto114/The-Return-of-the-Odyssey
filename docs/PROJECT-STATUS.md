# 项目开发进度（2026-09-17）

本记录对应 `feature/week2-platform`，其中多关和中文客户端的 `c845be0` 已推送；2026-09-17 的重开可靠性修复尚未推送。不代表 `main` 已包含这些改动，也不代表最终版验收通过。历史角色 D 进展及原始测试记录见 [D 角色项目进度](plans/D-PLATFORM-PROGRESS.md)；交付门槛以 [A / D 收尾清单](plans/WEEK2-AD-FINALIZATION.md) 为准。

本地验证：`server` 与 `bot` 分别执行 `go test ./...`、`go vet ./...` 通过；中文客户端 `odyssey_client` 和 `odyssey_logic_tests` 构建通过，CTest 4/4 通过。针对 2026-09-17 的修复，新增真实 TCP 双人失败注入测试：故意让新关卡启动失败，确认两人留在失败旧房间、不会收到虚假的 `MatchFound`，且可再次重试；正常重开保留“先 MatchFound、再 StageStarted”顺序。两条回归路径各连续运行 20 次通过。这些自动化结果不代替真人双客户端三关验收，也没有执行按项目约定暂缓的 Race。

| 范围 | 当前进度 | 证据或待验收事项 |
| --- | --- | --- |
| 联机基础与战斗 | 已接入服务端权威的双人匹配、首关战斗和机器人；仍需两名真人完整试玩 | 服务器和客户端可分别启动，两客户端曾建立本机连接；不能以连接成功替代实际玩法验收 |
| 奖励与关卡推进 | 功能分支已接通清场、按玩家发奖励、选择/超时默认、双方 Ready、Director 和下一关 | 真实 TCP 双人回归覆盖第 1→2 关；[10 Bot / 5 房间三关结果](verification/phase2-final/d2-ten-bot-three-stage-2026-09-16.json)为 10 成功、0 失败、共清除 30 关；真人三关尚未确认 |
| 玩家端体验与重开 | 完成中文界面和装备文案、奖励卡点击及数字键、失败后重开、可缩放窗口、双开独立日志；服务端重开会等待新关卡真正启动成功后才通知匹配完成 | 失败注入及重试的 TCP 回归通过，中文版四项 CTest 通过；新版 Windows 可执行文件需要按 [客户端构建说明](../client/README.md)从源代码生成，不提交本机 `build/` 产物；奖励点击、双方 Enter、拖动窗口与真实重开仍需真人复测 |
| D 平台模块 | 装备目录、Bot、指标与管理看板、Redis 恢复存储、MySQL 异步结算模块已交付 | 模块具备不等于正式入口已使用；终局和恢复仍有接线工作 |
| 正式终局与恢复 | 未完成最终端到端验收 | 第三关结束后的 `GameResult`/MySQL 持久化交付、正式 Resume 入口和令牌轮换需要完成并验证 |
| 容量与合并门槛 | 未完成 | 100 Bot / 50 房间持续 10 分钟、真实双客户端三关与数据库/恢复联调未验收；按此前约定，Race 留到开发完成、形成合并候选后再运行 |

## 下一步建议

1. 使用同一版本的 gameserver 和两份 `build/client-zh/client/odyssey_client.exe`，让两人实际完成奖励点击、各自按 Enter、三关通关和窗口缩放；把结果与失败日志写入验证记录。
2. 与入口负责人完成最终 `GameResult` 提交和 Resume 交接，分别验证胜利/失败结算、恢复令牌及断线阶段。
3. 跑 100 Bot / 50 房间持续负载、跨模块回归和干净环境构建；在合并候选阶段再执行 Race。所有门槛通过后才申请合入 `main`。

开发中的玩法说明见 [玩家操作指南](PLAYER-GUIDE.md)。本文件为阶段快照，后续状态变化应更新此页和相应验证证据。
