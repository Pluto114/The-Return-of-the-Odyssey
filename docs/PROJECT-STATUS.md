# 项目开发进度（2026-09-20）

本记录追踪 `feature/week2-platform` 的开发增量，包括多关、中文客户端、可靠重开、首次匹配、终局接线和本局积分榜。本次新增胜利/失败后的“远征飞行记录”、每关共享回血/武器拾取物，并修复客户端未编码药剂使用请求、奖励数值缺少玩家可见反馈的问题；这些仍是待真人验收的本地代码，未提交或推送。**开发基线同步不等于最终版验收或发布**，下面的待验收事项保持有效。历史角色 D 进展及原始测试记录见 [D 角色项目进度](plans/D-PLATFORM-PROGRESS.md)；交付门槛以 [A / D 收尾清单](plans/WEEK2-AD-FINALIZATION.md) 为准。

本地验证：`server` 与 `bot` 分别执行 `go test ./...`、`go vet ./...` 通过；本轮更新后的服务端 `go test ./...`、`go vet ./...` 通过；独立 `build/client-scoreboard` 的中文客户端构建通过，CTest 4/4 通过，新增计分测试覆盖伤害、承伤、击杀、倒地、重复清关事件、团队评分、排序和重置。此前胜利/失败重开的 TCP 回归连续运行 10 次通过。Dashboard `npm run build` 在此前验证通过。重开失败注入与正常重开回归此前各连续运行 20 次通过；新增首次匹配失败注入，防止关卡启动失败时误发 `MatchFound`，并验证成功时 `MatchFound → StageStarted` 顺序。终局的领域及真实 TCP 断线回归覆盖胜利/团灭/离弃、队友离开后的两人结果、重复提交和写入队列暂满时稳定的 `match_id`。独立端口上新构建的真实服务端复跑 [10 Bot / 5 房间三关](verification/phase2-final/d2-ten-bot-three-stage-2026-09-17.json)：10 成功、0 失败、30 次清关；这次明确关闭结果库和恢复开关，仅用于确认多关玩法无回归。这些自动化结果不代替真人双客户端三关、积分榜视觉验收或真实 MySQL 验收；按项目约定，Race 暂缓。

| 范围 | 当前进度 | 证据或待验收事项 |
| --- | --- | --- |
| 联机基础与战斗 | 已接入服务端权威的双人匹配、首关战斗和机器人；仍需两名真人完整试玩 | 服务器和客户端可分别启动，两客户端曾建立本机连接；不能以连接成功替代实际玩法验收 |
| 奖励与关卡推进 | 功能分支已接通清场、按玩家发奖励、选择/超时默认、双方 Ready、Director 和下一关；客户端现支持战斗中按 `Q` 使用药剂，并显示攻击力及权威数值变化提示 | 协议回归覆盖 `use_potion=true/false`，客户端 CTest 4/4 通过；服务端奖励/药剂测试覆盖攻击力提升及药剂单次消耗。真人仍需复测攻击伤害差异、受伤后按 `Q` 回血及满血误用提示体验 |
| 关卡地图拾取物 | 已实现每关刷新一个共享回血包和一个共享武器箱；靠近自动拾取，满血不消耗回血包，切关清除旧道具并生成新道具 | 服务端测试覆盖生成、回血、换武器、满血保护和切关刷新；快照协议传输完整道具集合与玩家装备 ID，客户端绘制、移除及中文反馈已通过无窗口测试。待真人双客户端确认抢拾同步和视觉尺寸 |
| 玩家端体验与重开 | 完成中文界面和装备文案、奖励卡点击及数字键、失败后重开、第三关通关后“再玩一把”、可缩放窗口、双开独立日志；服务端重开会等待新关卡真正启动成功后才通知匹配完成 | 胜利与失败终局复用双人房间重开流程；最新目录 `build/client-scoreboard` 的客户端已构建、CTest 4/4 通过。旧的 `build/client-zh` 与 `build/client-replay` 不包含积分榜。真人胜利后双人重开仍需复测。`build/` 为本地生成文件，不提交 |
| 本局积分榜 | 已完成客户端本局团队评分与双人贡献榜；按服务端可靠事件统计击杀、伤害、承伤、倒地、剩余生命和用时 | `MatchScoreboard` 为无窗口纯逻辑模块，客户端 CTest 4/4 通过。仍需真人验证两个客户端在胜利、团灭、重开后的数值和布局一致；断线后缺失事件不会由历史结果补发，历史/全服榜尚未实现 |
| D 平台模块 | 装备目录、Bot、指标与管理看板、Redis 恢复存储、MySQL 异步结算模块已交付 | 模块具备不等于正式入口已使用；终局和恢复仍有接线工作 |
| 正式终局与恢复 | 终局三种结果已接至本地写入器，仍未完成最终端到端验收 | 房间清场/团灭及全队离开可生成独立 `GameResult`，保留已离队玩家状态、稳定 `match_id`；新增队列持续满载回归，确认已复制结果的重试不会阻止空房间回收。尚未验证真实三关终局落库、MySQL 故障下的队列饱和与停服时未被队列接收的结果处置；正式 Resume 入口和令牌轮换未接通 |
| 容量与最终验收门槛 | 未完成 | 100 Bot / 50 房间持续 10 分钟、真实双客户端三关与数据库/恢复联调未验收；按此前约定，Race 留到开发完成、形成最终验收候选后再运行 |

## 下一步建议

1. 使用更新后的 gameserver 和两份 `build/client-scoreboard/client/odyssey_client.exe`，让两人实际完成奖励点击、各自按 Enter、三关通关、积分榜核对、胜利后“再玩一把”和窗口缩放；把结果与失败日志写入验证记录。
2. 启用真实 MySQL/Redis，跑三关的胜利/失败/离弃入库及数据库故障验收，确认队列饱和或停服时未被写入器接收的结果有可靠处置；完成正式 Resume 入口、令牌轮换和断线阶段回归。
3. 跑 100 Bot / 50 房间持续负载、跨模块回归和干净环境构建；在最终验收候选阶段再执行此前约定暂缓的 Race。所有门槛通过、用户验收后才申请最终发布，不能把本轮 `main` 开发基线同步视为发布验收。

开发中的玩法说明见 [玩家操作指南](PLAYER-GUIDE.md)。本文件为阶段快照，后续状态变化应更新此页和相应验证证据。
