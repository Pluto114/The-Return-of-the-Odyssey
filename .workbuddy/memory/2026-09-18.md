
## 2026-09-18
- 解答运行配置：项目无需额外安装（.tools 已就绪、build/client-windows 已有 odyssey_client.exe）。运行方式 = 根目录加载 scripts/env.ps1 后 go run ./server/cmd/gameserver（server/configs 为空，走内置默认 127.0.0.1:7777），客户端直接运行 build/client-windows/client/odyssey_client.exe（双开两个进程即可双人）。IDE 中当前 Run 配置是 npm install（tools/vs-install），与游戏无关，需改为 Go package ./server/cmd/gameserver（工作目录=仓库根）与 CMake application odyssey_client。
- 重新编译客户端：本机未安装 pwsh 7（宿主为 5.1），scripts/build/build.ps1 的 #requires -Version 7.0 不满足。改用自建 .tools/rebuild-client.bat：call .tools/vs-install/Common7/Tools/VsDevCmd.bat -arch=x64 -host_arch=x64，再设 PATH（cmake-3.31.12、ninja-1.13.2）、VCPKG_ROOT/VCPKG_VISUAL_STUDIO_PATH=.tools/vs-install、VCPKG_BINARY_SOURCES=files,.tools/vcpkg-cache，然后 cmake --preset client-windows && cmake --build --preset client-windows。在工具侧用 & $env:ComSpec /d /s /c 调用该 bat（直接 cmd /c 会被安全策略拦）。首次 32 分钟（vcpkg 34 个依赖全量重编，二进制缓存未命中），之后增量约 1 分钟。产物 build/client-windows/client/odyssey_client.exe 与 equipment.tsv 已更新。建议本机补装 pwsh 7。
